#include <mythos/render/vulkan/vulkan_pipeline.hpp>

namespace myth::vulkan {
    MYL_NO_DISCARD pipeline::pipeline(context& context)
        : m_context{ context } {}

    pipeline::~pipeline() {

    }
}
